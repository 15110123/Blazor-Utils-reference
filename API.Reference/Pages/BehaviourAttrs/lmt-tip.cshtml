@page "/behaviourAttr/lmttip"

<AttrDetail name="lmt-tip" type="0" minJsApi="0.1" minDomApi="Any" minCookieApi="Any" summary="Show a tooltip for the element." value="(required) Tooltip text content.">
    <p>Show a tooltip for the element when it is focused.</p>
    <Demo codes=@(new[]{"<label lmt-tip=\"This is the tooltip\">Hover me!</label>"})>
        <label lmt-tip="This is the tooltip">Hover me!</label>
    </Demo>
</AttrDetail>

@functions{
    protected override async Task OnAfterRenderAsync()
    {
        LMTBehaviours();
    }
}
